---
  root: /home/phyto/CoccoML/   # change this

  path_out: ModelOutput/

  traits: your_species_list.csv #path to list of species
  
  scale_X: True

  verbose: 1
  seed : 1 
  n_threads : 2 # how many cpu threads to use #change this
  cv : 10

  ensemble_config: 
    m1: 
      model : "rf"
      regressor : False
      classifier : True


  
  param_grid:
    rf_param_grid:
      reg_param_grid:
        regressor__n_estimators: [100]
        regressor__max_features: [2, 3, 4]
        regressor__max_depth: [3, 5]
        regressor__min_samples_leaf: [0.5, 0.8, 1]
        regressor__max_samples: [0.5, 0.8, 1] 

